# This workflow validates, deploys, and runs the specified bundle
# within a pre-production target named "dev".
name: 'Dev deployment'

concurrency: 1

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: 'Deploy bundle'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: databricks/setup-cli@main

      - run: databricks bundle deploy
        working-directory: dab_poc_gat
        env:
          DATABRICKS_HOST: https://adb-4256337347385801.1.azuredatabricks.net
          DATABRICKS_CLIENT_ID: ${{ secrets.CLIENT_ID }}
          DATABRICKS_CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
          DATABRICKS_TENANT_ID: ${{ secrets.TENANT_ID }}
          DATABRICKS_BUNDLE_ENV: dev

  pipeline_update:
    name: 'Run pipeline update'
    runs-on: ubuntu-latest
    needs:
      - deploy

    steps:
      - uses: actions/checkout@v3

      - uses: databricks/setup-cli@main

      - run: databricks bundle run my-job --refresh-all
        working-directory: dab_poc_gat
        env:
          DATABRICKS_HOST: https://adb-4256337347385801.1.azuredatabricks.net
          DATABRICKS_CLIENT_ID: ${{ secrets.CLIENT_ID }}
          DATABRICKS_CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
          DATABRICKS_TENANT_ID: ${{ secrets.TENANT_ID }}
          DATABRICKS_BUNDLE_ENV: dev
