# This is a Databricks asset bundle definition for dab_poc_gat.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_poc_gat
  uuid: 7004e59e-defa-43b0-bd15-7eb6ff8367b1

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    workspace:
      host: $DATABRICKS_HOST
      client_id: $DATABRICKS_CLIENT_ID
      client_secret: $DATABRICKS_CLIENT_SECRET
      azure_tenant_id: $DATABRICKS_TENANT_ID


  prod:
    mode: production
    workspace:
      host: https://adb-4256337347385801.1.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/cal.manish.sharma@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/cal.manish.sharma@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: cal.manish.sharma@gmail.com
        level: CAN_MANAGE
